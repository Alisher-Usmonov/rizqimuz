<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin | Homiylar</title>
    <script src="https://cdn.tailwindcss.com/"></script>
</head>
<body>
    <div class="w-full flex h-screen">
        <div class="w-[25%] bg-blue-300">
            <%- include("components/admin-bar") %>
        </div>
        <div class="w-[75%] bg-indigo-300 p-2 flex flex-col gap-1">
            <div class="grid grid-cols-2 gap-2 bg-indigo-400 p-2">
                <h1 class="col-span-2 text-center mb-2 font-semibold text-white text-xl">Sponsor yaratish</h1>
                <input type="url" name="website_url" id="sponsor-website-input" class="bg-white p-2 outline-none" spellcheck="false" placeholder="Website URL">
                <input type="file" name="image" id="sponsor-image-input" accept="image/*"  class="bg-white p-2 outline-none">
                <button id="submit-sponsor" class="w-full mt-1 col-span-2 rounded-md bg-indigo-500 py-2 text-white font-semibold text-lg">Yaratish</button>
            </div>
            <div class="w-full h-auto grid grid-cols-3 gap-1 bg-indigo-400 mt-3 p-2">
                <% sponsors.map(sponsor => { %>
                    <div class="bg-white p-2 flex flex-col">
                        <img class="border border-grary-400" src="<%= sponsor.image %>" />
                        <a href="<%= sponsor.website_url %>" class="font-semibold mb-1 text-lg"><%= sponsor.website_url %></a>
                        <button class="bg-red-400 py-1 text-white" id="delete-sponsor" data-sponsor-id="<%= sponsor.id %>">O'chirish</button>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
<%- include("parts/footer") %>